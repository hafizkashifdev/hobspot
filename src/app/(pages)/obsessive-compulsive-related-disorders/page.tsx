import { ObsessiveCompulsiveRelatedDisorders } from "@/features/obsessive-compulsive-related-disorders";

const ObsessiveCompulsiveRelatedDisordersPage = () => {
  return <ObsessiveCompulsiveRelatedDisorders />;
};

export default ObsessiveCompulsiveRelatedDisordersPage;
